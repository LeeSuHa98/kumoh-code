* MyArray라는 이름의 클래스 작성
* MyArray는 정수들을 저장하는 동적배열
* 멤버 변수로는 배열의 크기를 나타내는 size, 정수들이 실제로 저장된 메모리를 가리키는 ptr
* 멤버 함수로는 정수를 추가하는 append(), 마지막 정수를 삭제하는 delete(), 배열 안의 정수를 출력하는 print()
* MyArray 객체를 생성하여서 테스트